# 가상국가 모의전 웹사이트 MVP — Codex 개발 작업 지침서

> 문서 목적: 이 문서를 저장소 루트의 개발 기준으로 사용하고, Codex가 단계별로 구현할 수 있게 한다.  
> 대상: PC 우선 웹 MVP. 결제와 모바일 전용 UI는 제외한다.  
> 상태: 초기 기획안. 아직 정하지 않은 항목은 아래의 “MVP 기본 가정”을 따른다.

---

## 1. Codex에 내리는 최상위 지시

당신은 가상국가 모의전 웹서비스의 시니어 풀스택 개발자다. 이 프로젝트의 목표는 여러 사용자가 각자 국가를 맡아 정책·외교·연구 활동을 서술하고, 시스템과 관리자가 턴 단위로 결과를 판정하여 세계 상태에 반영하는 운영 가능한 MVP를 만드는 것이다.

다음 원칙을 항상 지킨다.

1. 한 번에 전체 서비스를 만들지 말고, 이 문서의 구현 단계 순서대로 작동하는 수직 기능을 완성한다.
2. 구현 전 저장소 상태와 기존 코드를 먼저 확인한다. 이미 작동하는 사용자 변경을 임의로 되돌리지 않는다.
3. 핵심 시뮬레이션 수치는 결정론적 코드가 계산한다. LLM은 판정 제안, 설명문, 사건, AI 국가 반응을 구조화된 데이터로 제안할 뿐 데이터베이스를 직접 수정하지 않는다.
4. 모든 LLM 결과는 스키마 검증, 범위 검증, 중복 실행 방지, 관리자 검토 절차를 거친다.
5. 인증·권한 검사는 화면 숨김이 아니라 모든 서버 변경 함수와 API에서 수행한다.
6. 경제·정치·연구·지도 상태 변경은 이전 상태와 변경 사유를 추적할 수 있어야 한다. 감사 로그와 턴 스냅샷을 남긴다.
7. 핵심 흐름에 가짜 버튼, 동작하지 않는 폼, 영구적인 TODO를 남기지 않는다. 아직 구현하지 않은 기능은 UI에 노출하지 않거나 “추후 지원”으로 명확히 표시한다.
8. 각 단계가 끝날 때 타입 검사, 린트, 단위 테스트, 관련 E2E 테스트를 실행하고 결과를 보고한다.
9. 라이브러리 버전과 외부 모델 ID를 추측하지 않는다. 구현 시 공식 문서와 실제 계정에서 사용 가능한 값을 확인하고 환경변수로 주입한다.
10. 모호하지만 MVP를 바꾸지 않는 세부사항은 본 문서의 기본값으로 진행한다. 데이터 손실, 보안, 비용, 운영 방식이 크게 달라지는 선택만 사용자에게 질문한다.

---

## 2. 제품 정의

### 2.1 핵심 개념

가상국가 모의전은 하나의 캠페인 세계 안에서 사용자가 국가를 맡아 운영하는 비동기식 정치·경제 시뮬레이션이다.

- 현실 시간의 일정 주기마다 게임 시간이 수개월~1년 진행된다.
- 사용자는 “금리를 인상한다”, “철도망을 건설한다”, “신형 반도체 공정을 개발한다”처럼 정책과 계획을 서술해 제출한다.
- 시스템은 국가의 현재 수치, 기술, 정치 상황, 과거 판정, 세계 사건을 입력으로 삼아 판정 초안을 만든다.
- 관리자는 초안과 예상 변경 내역을 검토하고 수정·승인·반려한다.
- 승인된 효과는 다음 턴 상태와 사건에 반영된다.
- 인간이 맡지 않은 국가는 AI가 외교 답변과 제한된 자율 행동을 제안한다.
- 표현 방식은 대전략 게임의 정보 밀도와 비주얼 노벨식 사건 연출을 결합하되, 기존 게임의 에셋·문구·고유 UI를 복제하지 않고 독자적인 디자인을 사용한다.

### 2.2 MVP의 성공 조건

관리자 1명과 플레이어 여러 명이 다음 한 사이클을 실제로 완료할 수 있으면 MVP가 성공한 것이다.

1. 사용자가 가입하고 국가 배정을 신청한다.
2. 관리자가 국가를 배정한다.
3. 사용자가 국가 현황을 보고 정책 연재와 외교 제안을 제출한다.
4. 관리자가 턴을 잠그고 자동 경제 계산과 LLM 판정을 실행한다.
5. 관리자가 모든 변경 차이를 검토하여 승인한다.
6. 턴이 공개되고 경제·정치·연구·사건·외교 상태가 갱신된다.
7. 채팅과 관리자 제재가 정상 작동한다.
8. 관리자가 헥사곤 영토를 수정하고 변경 이력을 남길 수 있다.

### 2.3 MVP 기본 가정

- 데이터 구조는 여러 캠페인을 지원하지만 초기 UI에서는 하나의 활성 캠페인만 운영한다.
- 게임 시간 진행량은 캠페인별 설정값이다. 기본값은 현실 1일당 게임 1년이지만, MVP에서는 관리자가 수동으로 턴 처리를 시작한다.
- 사용자 한 명은 한 캠페인에서 최대 한 국가만 담당한다. 공동 운영은 추후 기능이다.
- 국가는 관리자가 미리 만들거나, 배정된 사용자가 `빠른 국가 설정` 초안을 작성하고 관리자가 승인해 생성한다. 사용자 초안은 승인 전까지 시뮬레이션에 반영하지 않는다.
- 전쟁·병력·전술 전투 계산은 MVP 범위 밖이다. 전쟁 결과에 따른 영토 변경은 관리자가 지도 편집기로 반영할 수 있다.
- 경제 수치는 현실 예측 모델이 아니라 설명 가능하고 일관적인 게임 모델을 목표로 한다.
- AI 자동 판정은 기본적으로 관리자 승인 후 반영한다. 신뢰도가 낮거나 영향이 큰 효과는 자동 승인할 수 없다.
- 서비스 언어는 한국어를 기본으로 하고, 내부 코드·DB 필드·API는 영어로 작성한다.
- PC 기준 최소 화면 폭은 1280px, 주 목표는 1440×900 이상이다. 작은 화면에서는 읽기 전용 또는 축약 안내를 제공해도 된다.

### 2.4 MVP에서 제외할 것

- 결제, 구독, 유료 재화
- 네이티브 앱과 완전한 모바일 최적화
- 실시간 전투 및 군사 유닛 이동
- 관리자 승인 없이 국가·세계관·경제 수치를 즉시 확정하는 자유 편집기
- 음성 채팅, 영상 채팅, 파일 첨부 채팅
- 완전 무인 턴 공개
- 경제학 연구용 정밀 DSGE·CGE 모델
- 다국어 번역 시스템
- 복잡한 선거구 지도와 의석 자동 배분
- 대규모 공개 서비스용 분산 인프라

---

## 3. 사용자 역할과 권한

역할은 전역 역할과 캠페인 역할을 분리한다.

| 역할 | 주요 권한 |
| --- | --- |
| `USER` | 가입, 가이드 열람, 국가 배정 신청 |
| `PLAYER` | 배정 국가 현황 열람, 연재 제출, 외교, 채팅, 사건 선택 |
| `MODERATOR` | 채팅 삭제, 사용자 타임아웃, 신고 처리 |
| `ADMIN` | 국가·턴·경제·정치·연구·지도·배정·판정 전체 관리 |

권한 규칙:

- 플레이어는 자기 국가의 비공개 상세 데이터만 볼 수 있다.
- 타국 지도 패널에는 공개 정보, 외교 관계, 공개 사건만 표시한다.
- 관리자는 다른 국가로 가장하지 않는다. 관리자 수동 수정은 항상 관리자 작업으로 감사 로그에 기록한다.
- `ADMIN`만 턴 잠금, 판정 승인, 경제·정치 수치 수정, 영토 반영을 할 수 있다.
- `MODERATOR`는 게임 수치를 수정할 수 없다.
- 타임아웃된 사용자는 채팅 작성이 금지되지만 게임 현황 열람과 연재 작성 권한은 별도 제재가 없다. 필요하면 관리자가 계정을 정지한다.

---

## 4. 핵심 게임 루프와 턴 상태 머신

턴은 다음 상태를 가진다.

`DRAFT → LOCKED → CALCULATING → AI_RUNNING → REVIEW → PUBLISHED`

실패 시 `FAILED`로 이동하며 관리자가 안전하게 재개할 수 있어야 한다.

### 4.1 단계별 처리

1. `DRAFT`
   - 플레이어가 연재, 연구 배분, 사건 선택, 외교 제안을 제출한다.
   - 제출물은 마감 전까지 수정 가능하며 모든 버전을 기록한다.
2. `LOCKED`
   - 신규 제출과 수정이 금지된다.
   - 해당 턴의 입력 스냅샷과 제출물 ID 목록을 확정한다.
3. `CALCULATING`
   - 인구, GDP, 물가, 실업, 재정, 연구 포인트 같은 결정론적 기본 계산을 실행한다.
   - 계산 결과는 아직 공개 상태에 직접 반영하지 않고 턴 작업 공간에 저장한다.
4. `AI_RUNNING`
   - 정책 판정, 정치 판정, AI 국가 외교 답변, 야당 행동, 사건 초안을 작업 큐에서 생성한다.
5. `REVIEW`
   - 관리자가 국가별 변경 전/후, LLM 근거, 경고, 신뢰도, 실패 항목을 검토한다.
   - 수정·승인·반려할 수 있다.
6. `PUBLISHED`
   - 승인된 효과를 하나의 트랜잭션 단위로 반영한다.
   - 최종 국가 스냅샷과 공개 사건을 생성하고 게임 날짜를 전진시킨다.
   - 플레이어 알림을 생성한다.

### 4.2 필수 안전장치

- 각 턴 실행은 `campaign_id + turn_id + step` 기반의 idempotency key를 가진다.
- 이미 성공한 단계는 재실행해도 중복 반영되지 않는다.
- 턴 공개는 승인되지 않은 필수 판정이나 실패 작업이 남아 있으면 차단한다.
- 공개 직전 전체 변경 요약과 영향이 큰 수치 변화를 보여준다.
- 공개 후 값을 고치는 경우 기존 스냅샷을 덮어쓰지 않고 수정 리비전과 감사 로그를 만든다.
- 턴을 되돌리는 MVP 기능은 제공하지 않는다. 잘못된 공개는 다음 수정 리비전으로 보정한다.

---

## 5. 페이지와 기능 요구사항

### 5.1 공개·인증 페이지

#### `/login`, `/register`

- 이메일·비밀번호 로그인과 가입
- 가입 후 국가 배정 신청 안내
- 로그인 실패 사유는 계정 존재 여부를 과도하게 노출하지 않는다.

#### `/guide`

- 게임 진행 방식, 턴 주기, 연재 작성법, 판정 원칙, 외교 사용법
- 관리자가 마크다운 기반 콘텐츠를 수정할 수 있게 하거나 초기에는 저장소 콘텐츠로 관리한다.

#### `/lore`

- 배경, 역사, 세계관 연표, 주요 국가 소개
- 캠페인별 콘텐츠를 표시한다.

### 5.2 플레이어 공통 레이아웃

상단 바에는 현재 게임 날짜, 현재 턴 상태, 다음 마감, 사용자 국가, 알림을 표시한다. 왼쪽 내비게이션에는 대시보드·국가·연구·사건·연재·외교/지도·채팅을 둔다. 국가 영역 안에는 개요·역사/지리·인문·정치·경제 탭을 둔다.

국가 데이터는 제공된 전체 설정 양식을 그대로 한 화면에 나열하지 않는다. 모든 국가 페이지는 다음 2단계 정보 구조를 공통으로 사용한다.

1. 첫 화면에는 현재 판단에 중요한 핵심 정보 카드 최대 10개만 보여준다.
2. `세부 지표 보기`를 누르면 같은 페이지 아래에서 나머지 지표, 정의, 기준연도, 최근 추세, 계산 근거가 펼쳐진다.

세부 영역은 기본적으로 접혀 있어야 하며, 사용자가 마지막으로 선택한 펼침 상태를 브라우저 단위로 기억한다. 다른 국가의 공개 페이지에도 같은 구조를 적용하되 비공개 지표는 제외한다.

#### `/dashboard`

- 국기, 국명, 지도자, 정부 형태, 핵심 지표 요약
- GDP 성장률, 물가, 실업, 재정, 안정도, 정부 지지도, 연구력 카드
- 진행 중인 연구, 미처리 사건, 제출 마감, 새 외교 제안
- 최근 턴의 주요 변화 5개

#### `/country/overview`

- 핵심 10개: 국명, 국기, 수도, 정치체제, 국가원수, 총인구, 면적, 실질 GDP, 1인당 GDP, 공식 화폐
- 표어, 국가(國歌), 국목·국화·국조·국수, 최대도시, 국가코드 등은 `세부 정보 보기`에서 표시한다.
- 국가 전체를 한 번에 편집하거나 원본 양식을 텍스트로 노출하지 않는다.

#### `/country/setup`

- 국가가 아직 확정되지 않은 사용자에게만 `빠른 설정`을 제공한다.
- 빠른 설정의 필수 10개: 국명, 국기, 수도, 정치체제, 국가원수, 총인구, 면적, 실질 GDP, 공식 화폐/기준 화폐가치, 주요 산업
- 입력 후 시스템이 1인당 GDP, 인구밀도 등 파생값을 미리 계산해 보여준다.
- `고급 설정`을 펼치면 전체 국가 원장의 역사·상징·인문·정치·경제·외교 항목을 분야별로 입력할 수 있다.
- 사용자가 제출하면 `DRAFT → SUBMITTED → APPROVED/CHANGES_REQUESTED` 상태로 관리자가 검토한다.
- 이미 운영 중인 국가의 핵심 수치 변경은 일반 설정 저장이 아니라 연재 또는 관리자 수정 절차를 따른다.

#### `/country/history`

- 역사 서술, 행정구역 계층, 행성·수도·최대도시·면적·내수면 비율
- 첫 화면에는 수도, 최대도시, 면적, 행정구역 단계, 행성 등 핵심 정보만 표시한다.
- 전체 역사와 하위 행정구역 트리는 세부 영역에서 펼친다.

#### `/country/society`

- 핵심 10개: 총인구, 인구밀도, 출산율, 인구성장률, 기대수명, 평균연령, 해외 거주 자국인, 국내 거주 외국인, 공용어, 국교
- 디아스포라, 공용 문자, 군대, 상세 추세는 `세부 지표 보기`에서 표시한다.

#### `/country/politics`

- 핵심 10개: 정치체제, 국가원수, 총리/부통령, 의회 의장, 대법원장, 여당, 제1야당, 안정도, 정부 지지도, 사회 불안
- 전체 야당, 정당별 지지율·의석·이념 좌표, 정통성, 국가 역량, 부패, 민주성은 `세부 지표 보기`에서 표시한다.
- 활성 정치 수정치, 만료 턴, 최근 정치 판정과 야당 행동 기록을 세부 영역에 연결한다.

#### `/country/economy`

- 핵심 10개: 실질 GDP, 1인당 GDP, 실질 GDP 성장률, 소비자물가 변동률, 실업률, 정부지출, GDP 대비 부채율, 외환보유고, 공식 화폐/화폐가치, 국가신용등급
- GDP 디플레이터, 실질 GNI/GNP, 1인당 GNI, 국부, 소득·자산 지니계수, 토지가 변동률, 정부지출 증가율, 주요 은행·대기업·산업은 `세부 지표 보기`에서 표시한다.
- 명목 GDP, 재정수지, 기준금리, 경상수지 등 계산 엔진에 필요한 보조 지표도 세부 영역에 함께 표시할 수 있다.
- 최근 8턴 추세 그래프와 지표별 설명·계산 기여도 툴팁을 제공한다.
- 숫자에는 단위, 기준 시점, 명목/실질 여부를 반드시 표시한다.

#### `/country/research`

- 확대·축소 가능한 DAG형 기술 트리
- 노드 상태: 잠김, 연구 가능, 진행 중, 완료
- 선행 기술, 비용, 예상 기간, 효과, 설명
- 턴별 연구 포인트 배분과 현재 진행도
- 순환 선행 조건을 생성할 수 없게 검증한다.

#### `/events`

- 현재 처리해야 할 사건, 진행 중 사건, 지난 사건 탭
- 비주얼 노벨형 사건 카드: 독자 제작 배경/인물 이미지 영역, 제목, 본문, 선택지, 예상 가능한 범위의 효과
- 선택은 마감 전 1회 변경 가능 여부를 사건 설정으로 제어한다.
- 선택하지 않은 필수 사건이 있으면 턴 마감 경고를 표시한다.

#### `/submissions`

- 새 연재 작성: 제목, 분야, 본문, 목표, 예상 기간, 관련 예산, 관련 국가/기술
- 분야: 경제, 정치, 연구, 사회, 외교, 기타
- 상태: `DRAFT`, `SUBMITTED`, `LOCKED`, `JUDGING`, `NEEDS_INFO`, `APPROVED`, `REJECTED`, `PUBLISHED`
- 본문은 마크다운 일부만 지원하며 HTML은 허용하지 않는다.
- 제출 이력, 관리자 질문, 판정문, 실제 반영 효과를 보여준다.
- LLM에게 보내기 전 글자 수와 토큰 상한을 적용한다.

#### `/diplomacy`

- 세계 지도에서 국가를 클릭하면 공개 정보와 양국 관계 패널을 연다.
- “외교 제안 보내기”에서 유형, 제목, 내용, 공개/비공개, 유효기간을 입력한다.
- 유형: 성명, 협상, 조약 제안, 무역 제안, 원조, 경고, 기타
- 인간 담당 국가는 수신함에서 수락·거절·수정 제안할 수 있다.
- AI 국가는 비동기 작업으로 답변 초안을 만들고 규칙 검증 후 발송한다.
- 관계도는 `-100~100` 숫자와 상태 태그를 함께 표시한다.

#### `/chat`

- MVP 채널: 캠페인 전체 채널, 국가 내부 채널, 운영 공지 채널
- 최근 메시지 페이지네이션, 실시간 새 메시지 알림, 답글 1단계
- 메시지 전송 속도 제한, 최대 길이, XSS 방지
- 삭제된 메시지는 본문 대신 삭제 표시를 남기고 관리자 감사 로그를 보존한다.
- 타임아웃 중에는 남은 시간을 표시한다.

### 5.3 관리자 페이지

#### `/admin`

- 현재 턴 진행 상황, 미처리 제출, 실패한 AI 작업, 미배정 사용자, 신고 수
- 턴 잠금/계산/AI 실행/검토/공개 버튼은 상태에 맞는 것만 활성화한다.
- 위험 작업은 예상 대상 수와 결과를 확인하는 2단계 확인을 사용한다.

#### `/admin/countries` 및 `/admin/countries/[countryId]`

- 국가 생성, 사용자 작성 초안 검토, 국가 원장 전체 편집
- 편집 탭: 상징, 역사/행정구역, 지리, 인문, 정치, 경제, 단위, 외교, 지도
- `빠른 설정`에는 플레이어와 같은 필수 10개만 표시하고, `고급 설정`에서 전체 필드를 편집한다.
- 파생 지표는 읽기 전용으로 자동 계산하고 원본 입력과 불일치하면 저장 전에 경고한다.
- 초기 국가 설정 승인 시 `baseline_turn` 스냅샷을 생성한다.
- 운영 시작 후 경제·인구·정치 수치를 바꾸면 일반 프로필 수정이 아니라 턴 수정 제안과 감사 로그를 만든다.

#### `/admin/economy`

- 국가를 행, 핵심 경제 지표를 열로 표시하는 고밀도 테이블
- 검색, 필터, 고정 열, 다중 선택, 일괄 수정
- 편집값은 즉시 반영하지 않고 변경 전/후 diff를 만든다.
- 범위 오류, 단위 오류, 비정상 급변을 경고한다.
- 국가 상세 드로어에서 전체 지표, 수정치, 계산 근거를 확인한다.

#### `/admin/politics`

- 국가별 지도자, 정부 형태, 안정도, 정통성, 지지도, 불안도 일괄 보기
- 정당 추가/수정, 지지율·의석 편집, 여야 설정
- 정당 지지율 합계는 100%가 되도록 검증하거나 명시적 “기타”에 차이를 배분한다.
- 수동 수정은 사유 입력을 필수로 한다.

#### `/admin/submissions`

- 처리 대기열, 국가/분야/위험도/상태 필터
- 3열 검토 화면: 원문 및 국가 문맥 / AI 판정 근거 / 수치·사건 변경 diff
- 판정 재생성, 수동 수정, 승인, 반려, 추가 정보 요청
- 재생성은 기존 결과를 삭제하지 않고 새 `judgment_run`으로 남긴다.

#### `/admin/map`

- MapLibre 기반 헥사곤 지도
- 도구: 단일 선택, 브러시, 지우개(미소유화), 국가 선택, 확대/축소, 실행 취소/다시 실행
- 셀 위에 국가색, 해양/육지, 잠금 여부를 표시한다.
- 저장 전 변경 셀 수와 `A국 → B국` 요약을 보여준다.
- 모든 저장은 하나의 변경 세트와 새 지도 리비전을 만든다.
- 클라이언트가 임의 geometry를 보내게 하지 말고 서버에 존재하는 `cell_id` 목록만 수정한다.

#### `/admin/users`

- 가입 사용자 검색, 역할, 상태, 국가 배정 신청, 최근 접속
- 국가 배정·해제, 역할 변경, 계정 정지
- 한 국가 중복 배정을 막는다.

#### `/admin/moderation`

- 신고·삭제 메시지·타임아웃 기록
- 메시지 소프트 삭제, 타임아웃 시작/해제, 계정 정지
- 조치 사유와 만료 시각을 필수로 기록한다.

#### `/admin/ai-jobs`

- 대기·실행·성공·실패·재시도 작업
- 공급자, 모델 별칭, 지연시간, 입력/출력 토큰, 예상 비용, 오류 분류
- 원문 프롬프트에는 비밀키가 절대 포함되지 않으며 민감 로그 표시 권한은 관리자에게만 준다.

---

## 6. 시뮬레이션 설계

### 6.1 시간

- `campaign.start_game_date`: 캠페인 시작 게임 날짜
- `campaign.months_per_turn`: 기본 12, 관리자 설정 가능
- `turn.sequence`: 1부터 증가
- `turn.game_date_start`, `turn.game_date_end`
- 현실 스케줄은 마감 알림에만 사용하고, 실제 상태 전진은 관리자 공개 작업이 수행한다.

### 6.2 국가 원장과 표시 계층

국가 설정 양식의 전체 항목은 단순 소개문이 아니라 국가 원장과 시뮬레이션의 초기 상태로 저장한다. 단, 화면에는 원장을 그대로 출력하지 않고 분야별 핵심 정보와 세부 정보로 나눈다.

#### 국가 원장 필드

| 분야 | 저장 항목 | 시뮬레이션/표시 용도 |
| --- | --- | --- |
| 국가상징 | 국명, 국기, 표어, 국가(國歌), 국목, 국화, 국조, 국수 | 국가 정체성과 공개 프로필. 국가는 텍스트·음원 메타데이터를 분리 가능 |
| 역사 | 역사 본문, 주요 연표 | LLM 판정의 세계관 문맥과 공개 역사 페이지 |
| 행정구역 | 제1·2·3단계 명칭과 실제 구역 트리 | 지역 사건, 수도권/지방 정책의 대상 |
| 지리 | 행성, 수도, 최대도시, 총면적, 내수면 비율 | 인구밀도, 물류·개발·환경 판정의 입력 |
| 인문 | 총인구, 해외 거주 자국인, 국내 거주 외국인, 디아스포라, 인구밀도, 출산율, 인구성장률, 기대수명, 평균연령, 공용어·문자, 국교, 군대 | 노동력·내수·인구 변화와 정치·사회 판정의 입력 |
| 정치 | 정치체제, 국가원수, 총리/부통령, 상·하원 의장, 대법원장, 여당, 야당 | 정치 페이지, LLM 정치 문맥, 정당 시뮬레이션의 기준 |
| 경제 | 실질 GDP, 1인당 GDP, 성장률, GDP 디플레이터, 실질 GNI·1인당 GNI·실질 GNP, 국부, 외환보유고, 화폐/가치, 신용등급, 소득·자산 지니계수, CPI 변동률, 토지가 변동률, 실업률, 부채율, 은행·대기업, 정부지출·증가율, 주요 산업 | 다음 턴 경제 계산의 상태 벡터와 판정 문맥 |
| 단위 | 법정연호, 시간대, 도량형, ccTLD, 국가코드, 차량 통행 방향 | 날짜·단위·프로필 표시와 코드 검증 |
| 외교 | 외교 성향, 수교국, 동맹국, 국제기구 | 외교 관계 초기값과 AI 국가 성향 문맥 |
| 지도 | 지도 리비전과 소유 헥스 | 영토·면적·인접국 계산. 이미지 한 장으로 저장하지 않음 |

`국가`라는 필드명은 국명과 혼동되므로 UI와 DB 설명에서 `국가(國歌)` 또는 `national_anthem`으로 표기한다. 해외 거주 자국인은 현재 국적자, 디아스포라는 국적과 무관한 혈통·문화 공동체로 구분한다.

#### 입력값과 파생값

다음 값은 원장에 표시되지만 다른 원본값에서 자동 계산하는 것을 기본으로 한다.

- 인구밀도 = 총인구 ÷ 육지면적
- 1인당 GDP = 실질 GDP ÷ 총인구
- 1인당 GNI = 실질 GNI ÷ 총인구
- 정부지출 증가율 = 현재 정부지출 ÷ 전기 정부지출 - 1
- GDP 대비 부채율 = 국가채무 ÷ GDP
- 육지면적 = 총면적 × (1 - 내수면 비율)

빠른 설정에서 사용자가 파생값을 함께 입력한 경우 계산값과 비교하여 오차를 보여준다. 일정 허용오차를 넘으면 원본값을 수정하게 하며, 관리자가 특별한 세계관 사유로 override할 때는 사유와 적용 턴을 기록한다.

#### 분야별 단계적 공개

- 국가 개요, 인문, 정치, 경제 페이지는 각각 핵심 카드 최대 10개만 첫 화면에 표시한다.
- `세부 지표 보기`는 나머지 원장 항목과 최근 추세를 펼친다.
- `계산 근거 보기`는 해당 지표가 다음 턴 계산에 미친 기여도를 별도로 표시한다.
- 국가 설정은 `빠른 설정`과 `고급 설정`으로 나누며, 빠른 설정만으로도 유효한 초기 스냅샷을 만들 수 있어야 한다.
- 값이 없는 세부 필드는 빈 줄로 노출하지 않고 숨기며, 관리자에게만 미입력 배지를 표시한다.

### 6.3 거시경제

#### 상태 벡터

사용자가 국가 설정에서 기술한 경제·인문 수치를 초기 상태로 사용하고, 이후 매 턴 동일한 필드를 새 스냅샷으로 계산한다.

- 인구, 출산율, 인구성장률, 평균연령, 기대수명
- 실질 GDP, 명목 GDP, 1인당 GDP, 실질 성장률, GDP 디플레이터
- 실질 GNI, 1인당 GNI, 실질 GNP, 국부
- 외환보유고, 공식 화폐, 기준 화폐가치와 환율 지수
- 국가신용등급과 등급기관
- 소득 지니계수, 자산 지니계수
- 소비자물가지수 변동률, 토지가 변동률, 실업률
- 정부 수입·지출, 정부지출 증가율, 재정수지
- 국가채무와 GDP 대비 부채율
- 기준금리, 경상수지/GDP, 생산성 지수
- 주요 산업별 비중·생산성·성장률
- 주요 대형 은행과 대기업의 시스템 중요도·건전성·산업 태그

비율은 DB에 소수로 저장한다. 예: 3.5%는 `0.035`. 화면에서만 퍼센트로 바꾼다. 금액은 부동소수점이 아니라 PostgreSQL `numeric`과 서버의 Decimal 연산을 사용한다. 모든 GDP·GNI·GNP·국부·정부지출·외환보유고에는 기준연도, 가격 기준, 통화, 배율을 함께 저장한다.

국가 내부 계산용 현지 통화 값과 국가 간 비교용 캠페인 기준 화폐 환산값을 함께 만들며, 환산에는 해당 턴의 공식 화폐가치를 사용한다. 신용등급 원문은 보존하고 계산 엔진에서는 캠페인 규칙에 정의된 점수로 변환한다.

#### 입력값이 계산에 미치는 영향

| 입력군 | 다음 턴 계산에서의 주요 역할 |
| --- | --- |
| 인구·출산율·인구성장률·연령·기대수명 | 노동공급, 잠재성장률, 내수, 복지지출 압력 |
| 실질 GDP·기존 성장률·실업률 | 경기 위치, 생산 갭, 성장률과 고용 변화의 출발점 |
| GDP 디플레이터·CPI 변동률 | 명목 GDP, 물가 관성, 실질/명목 값 변환 |
| GNI·GNP와 GDP의 차이 | 해외 요소소득, 송금, 해외 자산·다국적 기업 충격 노출 |
| 국부·자산 지니·토지가 변동률 | 자산효과, 소비, 금융불안, 부동산 과열·침체 사건 |
| 외환보유고·화폐가치·신용등급 | 환율 방어력, 해외 조달금리, 외환·채무 위기 위험 |
| 소득 지니·실업률 | 소비성향, 세수, 정부 지지도와 사회 불안 |
| 정부지출·증가율·부채율 | 재정 충격, 이자 부담, 민간 수요 구축/구축 효과 |
| 주요 산업 | 부문별 성장 기여, 공급망·무역·기술 충격 전달 |
| 주요 은행·대기업 | 금융·기업 사건의 전염도. 이름만으로 효과를 주지 않고 구조화된 중요도 태그를 사용 |

각 입력이 실제로 0의 계수로 무시되지 않도록 계산 규칙과 단위 테스트에 영향 경로를 둔다. 다만 중복 지표를 단순 합산해 같은 효과를 두 번 반영하지 않는다. 예를 들어 GDP 성장률과 실업률은 하나의 경기 상태를 추정하는 데 함께 쓰고, GDP 디플레이터와 CPI는 각각 전체 생산 가격과 소비자물가 역할을 구분한다.

#### 기본 계산 순서

캠페인별 계수를 설정할 수 있게 하되 첫 시드에서는 아래의 설명 가능한 단순 모델을 사용한다.

1. 입력 정합성 확인
   - 파생값을 다시 계산하고 단위·기준연도·통화가 일치하는지 확인한다.
   - 누락값은 캠페인 기본값 또는 관리자 승인 추정값으로 채우고 `estimated=true`를 남긴다.
2. 인구와 노동력
   - 전기 인구, 출산율, 사망을 대리하는 기대수명·평균연령, 순이동 효과로 다음 인구와 노동공급을 계산한다.
3. 잠재성장률
   - 노동 성장, 생산성, 산업 구조, 기술, 자본/인프라 수정치의 합으로 계산한다.
4. 경기·수요 충격
   - 정부지출 증가, 실업 갭, 실질금리, 자산가격, 무역·외교 충격을 반영한다.
5. 실질성장률
   - `잠재성장률 + 경기순환효과 + 재정효과 + 구조개혁효과 + 외부충격`
   - 일반 상황의 기본 범위를 `-25%~30%`로 제한하고, 초과는 관리자 승인을 요구한다.
6. 물가와 자산가격
   - 전기 CPI·GDP 디플레이터의 관성, 생산 갭, 환율/수입물가, 공급 충격으로 계산한다.
   - 토지가격은 성장·금리·신용·정책 효과에 반응하되 소비자물가와 분리한다.
7. 고용과 분배
   - 성장률과 잠재성장률 차이로 실업을 계산한다.
   - 세금·고용·자산가격·정책 효과로 소득/자산 지니계수를 완만하게 이동시킨다.
8. 국민소득과 국부
   - 실질 GDP와 해외 요소소득을 이용해 GNI/GNP를 갱신한다.
   - 저축, 투자, 자산가격, 파괴·재건 충격을 국부에 반영한다.
9. 재정과 신용
   - 정부 수입은 GDP, 실효세율, 징수 효율에서 계산하고 지출은 기본 지출과 승인 정책 비용을 합산한다.
   - 재정수지, 채무, 부채율을 계산한 뒤 성장·외환·채무 부담을 이용해 신용등급 변경 후보를 만든다.
10. 대외 부문과 화폐
   - 경상수지, 외환보유고, 화폐가치/환율은 무역, 자본 흐름, 신용등급, 외교·제재·원조 효과를 반영한다.
11. 산업·금융 전파
   - 산업별 성장 기여도를 합산하고, 시스템 중요 은행·기업의 위기는 연결 산업과 국가 지표에 제한된 충격을 준다.

명목 GDP는 실질 GDP와 GDP 디플레이터에서 일관되게 산출한다. 1인당 값, 인구밀도, 지출 증가율, 부채율 같은 파생값은 최종 단계에서 다시 계산한다.

계수는 “현실의 보편 법칙”으로 하드코딩하지 말고 `simulation_rules`에 버전별 JSON으로 저장한다. 계산 결과마다 사용한 규칙 버전, 원본값/추정값 여부, 주요 기여도를 기록해 관리자와 플레이어가 설명을 볼 수 있게 한다.

#### 경제 판정 역할 분담

- 코드: 기초 성장, 물가, 실업, 재정, 채무, 기술 효과, 기간 만료를 계산한다.
- LLM: 사용자의 서술이 어떤 표준 효과로 번역되는지, 성공/지연/부분 성공 여부, 부작용, 사건 초안을 제안한다.
- 관리자: LLM 제안의 수치와 기간을 수정하거나 승인한다.

### 6.4 정치

핵심 지표는 모두 `0~100` 범위로 통일한다.

- 안정도 `stability`
- 정통성 `legitimacy`
- 정부 지지도 `government_approval`
- 사회 불안 `unrest`
- 국가 역량 `state_capacity`
- 부패 `corruption`
- 민주성 `democracy`

정당은 이름, 색상, 이념 좌표, 의석, 지지율, 여야 상태, 조직력, 자금, 주요 인물을 가진다. MVP 이념 좌표는 경제 좌우 `-100~100`, 사회 자유/권위 `-100~100` 두 축을 사용한다.

정치 효과는 표준 수정치로 변환한다. 예:

- 정부 지지도 `+3` 포인트, 2턴
- 야당 조직력 `+5` 포인트
- 사회 불안 `+4` 포인트
- 특정 정당 지지율 이동 `±2` 포인트

정당 지지율을 바꾼 뒤 총합을 100%로 정규화하되, 고정된 정당이나 관리자가 지정한 최소 지지율을 침범하지 않는다. 선거와 연정 자동 계산은 후속 범위로 둔다.

#### 야당 자율 행동

- 턴마다 국가별로 최대 1개의 야당 행동을 생성한다.
- 트리거는 낮은 정부 지지도, 높은 불안, 야당 조직력, 최근 사건, 선거 예정 여부를 사용한다.
- LLM은 행동 서술과 표준 효과를 제안한다.
- 쿠데타, 정권 붕괴, 내전처럼 체제 전환을 일으키는 행동은 항상 `requiresAdmin=true`다.
- 행동 결과는 일반 사건으로 플레이어에게 공개한다.

### 6.5 연구 테크트리

- 기술 노드는 방향성 비순환 그래프(DAG)다.
- 필드: 코드, 이름, 분야, 시대, 설명, 비용, 선행 노드, 상호배타 그룹, 효과 템플릿, 이미지 키, 공개 여부
- 국가별 상태: 잠김, 가능, 진행, 완료, 진행 포인트, 시작/완료 턴
- 국가의 기본 연구 포인트는 인구가 아니라 교육·연구 투자·생산성·국가 역량을 중심으로 계산한다.
- 완료 효과는 allowlist 기반의 표준 수정치만 적용한다.
- 관리자는 노드를 추가·수정할 수 있지만, MVP에서는 시각적 트리 편집기 대신 폼과 선행 기술 다중 선택으로 충분하다.
- 저장 시 순환 검사, 중복 코드, 음수 비용, 존재하지 않는 선행 노드를 검증한다.

### 6.6 사건과 비주얼 노벨 요소

사건은 다음 요소를 가진다.

- 제목, 짧은 부제, 본문
- 배경 이미지 키, 인물 초상 키, 음악 키(음악 재생은 MVP 이후 가능)
- 공개 범위: 전체, 특정 국가, 관리자 전용
- 시작·만료 턴
- 선택지와 선택 결과
- 자동 트리거 또는 관리자 발행

트리거는 임의 JavaScript가 아니라 제한된 JSON 조건 DSL을 사용한다. 예: `all`, `any`, `metric`, `gte`, `lte`, `hasTech`, `hasFlag`. 효과도 allowlist 기반 JSON patch가 아니라 도메인 명령 목록으로 저장한다.

### 6.7 외교와 AI 국가

국가 간 관계는 수치 `-100~100`, 태그, 조약, 최근 상호작용으로 구성한다.

AI 국가 답변 파이프라인:

1. 상대 제안, AI 국가의 공개/비공개 성향, 현재 관계, 조약, 경제·정치 상황을 모은다.
2. LLM이 `ACCEPT`, `REJECT`, `COUNTER`, `DELAY`, `NEEDS_ADMIN` 중 하나와 답변문, 관계 변화 제안을 만든다.
3. 조약 체결, 영토, 대규모 원조, 전쟁, 체제 변화가 포함되면 관리자 검토로 보낸다.
4. 낮은 영향의 성명과 단순 거절은 캠페인 설정에 따라 자동 발송할 수 있다. MVP 기본값은 관리자 검토다.

AI 국가는 알지 못하는 비공개 타국 정보를 입력으로 받지 않는다. 국가별 성향 프롬프트는 공개 이념, 국익, 금기, 위험 선호, 외교 목표를 구조화한 데이터로 관리한다.

### 6.8 헥사곤 지도

MVP 지도는 PostGIS의 평면 헥사곤 격자를 사용한다.

- 기준 좌표계: 저장/계산용 Web Mercator `EPSG:3857`, 외부 표시용 `EPSG:4326`
- 기본 변 길이: 20km. 캠페인 생성 시 조정 가능하지만 생성 후 변경 금지
- 육지 판정: 셀 중심점이 육지 폴리곤 내부인지로 초기 분류
- 셀 geometry는 불변이고, 국가 소유권만 리비전별로 변경한다.
- 전 세계 셀을 한 번에 브라우저로 보내지 않는다.
- 읽기 지도는 벡터 타일(`ST_AsMVT`) 또는 줌별 서버 타일 엔드포인트로 제공한다.
- 관리자 편집 요청은 `cell_id[]`, 목표 국가, 예상 지도 리비전을 보낸다.
- 예상 리비전이 현재와 다르면 충돌로 거절하고 새 데이터를 불러오게 한다.
- 지도 변경 세트에는 이전/새 소유국, 작업자, 사유, 생성 시각을 기록한다.

---

## 7. LLM 판정 시스템

### 7.1 중요한 배포 전제

OpenCode, Cursor, Codex는 개발 도구이지 배포된 웹사이트의 런타임 API가 아니다. 실제 서비스의 백엔드 작업자는 서버에서 호출 가능한 DeepSeek 또는 OpenAI API 자격 증명을 사용해야 한다. 로컬에서 OpenCode를 중계 서버처럼 상시 실행하는 구조를 MVP 기본 설계로 삼지 않는다.

OpenAI의 공식 API 모델 ID `gpt-5.6-luna`와 `gpt-5.6-terra`를 사용할 수 있다. Luna는 저비용·고처리량 작업에 최적화된 모델이고, Terra는 성능과 비용의 균형을 위한 모델이다. 이 프로젝트에서는 사용 목적에 맞춰 Luna를 DeepSeek의 대체 판정 모델로, Terra를 고난도 판정 모델로 사용한다.

모델 역할은 다음과 같이 설정하되, 향후 모델 교체가 가능하도록 실제 ID는 환경변수에 둔다.

- `JUDGMENT_DEFAULT_PROVIDER` / `JUDGMENT_DEFAULT_MODEL`: 일반 판정. DeepSeek 계열을 기본값으로 설정 가능
- `JUDGMENT_FALLBACK_PROVIDER=openai` / `JUDGMENT_FALLBACK_MODEL=gpt-5.6-luna`: DeepSeek의 공급자 거절, 지역·정치적 주제 처리 불가, 스키마 오류 반복 시 대체 판정
- `JUDGMENT_COMPLEX_PROVIDER=openai` / `JUDGMENT_COMPLEX_MODEL=gpt-5.6-terra`: 여러 국가·경제 부문이 얽힌 고난도 판정과 낮은 신뢰도 결과의 재검토
- `JUDGMENT_CHEAP_PROVIDER` / `JUDGMENT_CHEAP_MODEL`: 요약, 분류, 제목 생성

`gpt-5.6-luna`와 `gpt-5.6-terra`는 공식 API ID다. 그래도 코드에 문자열을 여러 번 하드코딩하지 않고 중앙 설정과 실행 로그에 실제 ID를 기록한다. 배포 계정의 접근 권한과 사용량 한도는 배포 전에 API 콘솔에서 확인한다.

### 7.2 공급자 추상화

`AIProvider` 인터페이스를 만든다.

```ts
interface AIProvider {
  generateStructured<T>(input: {
    model: string;
    system: string;
    payload: unknown;
    schema: JsonSchema;
    timeoutMs: number;
    idempotencyKey: string;
  }): Promise<AIResult<T>>;
}
```

구현체는 `OpenAIProvider`, `DeepSeekProvider`, `FakeAIProvider`로 분리한다. OpenAI 구현은 Responses API의 Structured Outputs를 사용한다. 다른 공급자가 JSON Schema 강제를 지원하지 않으면 JSON 출력 요청 후 동일한 서버 스키마로 검증하고 최대 1회 복구 요청한다.

### 7.3 판정 출력 스키마

모든 경제·정치 판정은 최소한 다음 구조를 따른다.

```ts
type JudgmentProposal = {
  verdict: "SUCCESS" | "PARTIAL" | "FAILURE" | "DELAYED" | "NEEDS_ADMIN";
  publicSummary: string;
  publicNarrative: string;
  adminRationale: string;
  assumptions: string[];
  confidence: number; // 0..1
  effects: Array<{
    targetType: "COUNTRY" | "PARTY" | "RELATION" | "RESEARCH";
    targetId: string;
    metric: string;
    operation: "ADD" | "MULTIPLY";
    value: number;
    durationTurns: number | null;
    reason: string;
  }>;
  followUpEvents: Array<{
    title: string;
    summary: string;
    visibility: "PUBLIC" | "COUNTRY" | "ADMIN";
  }>;
  warnings: string[];
  requiresAdmin: boolean;
};
```

LLM에게 `SET`, 임의 SQL, 임의 JSON Patch 권한을 주지 않는다. 허용 metric, 연산, 1회 변화량, 지속 기간은 서버 레지스트리로 검증한다. 범위를 넘는 효과는 삭제하지 말고 관리자 경고로 격리한다.

### 7.4 판정 입력

필요한 정보만 최소화해 전달한다.

- 캠페인 규칙 버전과 현재 게임 날짜
- 제출 원문과 구조화된 메타데이터
- 해당 국가의 관련 지표와 최근 추세
- 관련 기술, 수정치, 최근 판정
- 직접 관련된 타국·세계 사건
- 표준 효과 사전과 허용 범위

전체 DB, 모든 채팅, 다른 국가의 비공개 정보를 보내지 않는다. 사용자 원문은 “신뢰할 수 없는 시뮬레이션 입력”으로 JSON 안에 넣고, 원문 속 지시가 시스템 규칙을 바꿀 수 없다고 시스템 프롬프트에 명시한다.

### 7.5 작업 파이프라인

1. `submission`과 입력 스냅샷을 고정한다.
2. 과제 복잡도를 규칙 기반으로 분류한다.
3. 기본 공급자를 호출한다.
4. 출력 스키마와 도메인 범위를 검증한다.
5. 거절, 타임아웃, 5xx, 빈 출력, 정치적 주제 처리 불가, 스키마 2회 실패를 오류 코드로 분류한다.
6. 재시도 가능한 오류는 지수 백오프로 최대 2회 재시도한다.
7. 공급자 정책상 처리 불가 또는 반복 스키마 실패는 원문을 왜곡하거나 검열 우회하지 않고 대체 공급자로 전환한다.
8. 고난도 또는 낮은 신뢰도 결과는 복잡 작업 모델로 한 번 재검토할 수 있다.
9. 결과를 `judgment_run`과 `judgment_proposal`에 저장한다.
10. 관리자가 diff를 승인할 때만 도메인 명령으로 변환한다.

중요: 특정 정치적 역사 소재 때문에 한 공급자가 답변을 거절해도, 공급자의 안전 장치를 우회하는 프롬프트를 만들지 않는다. 공급자 라우팅과 관리자 수동 판정으로 정상 처리한다.

### 7.6 모델이 직접 결정하면 안 되는 것

- 인증·권한
- 사용자 제재
- 실제 DB ID 생성
- 턴 공개
- 지도 geometry 수정
- 화폐 단위 변환
- 최종 경제 산술
- 관리자 승인 생략 여부
- 비밀정보 공개 범위

### 7.7 비용·품질 기록

각 실행에 다음을 기록한다.

- 공급자와 실제 모델 ID
- 프롬프트 버전
- 입력 스냅샷 해시
- 시작/종료 시각과 지연시간
- 입력/출력 토큰
- 공급자가 제공하면 비용 또는 비용 계산용 사용량
- 종료 상태와 오류 분류
- 원본 결과와 검증된 결과

동일 idempotency key의 성공 결과가 있으면 다시 과금 호출하지 않는다. 관리자가 “재생성”을 누르면 새 시도 번호를 명시적으로 만든다.

---

## 8. 권장 기술 구조

### 8.1 스택

- 프론트엔드/서버: Next.js App Router + TypeScript
- UI: Tailwind CSS + 접근 가능한 헤드리스 컴포넌트. 디자인 시스템 토큰은 직접 정의
- DB: PostgreSQL + PostGIS
- ORM/마이그레이션: Drizzle ORM. 복잡한 공간 쿼리와 벡터 타일은 검토된 raw SQL 사용
- 인증: Better Auth의 이메일·비밀번호 세션. 초기에는 이메일 인증을 개발 설정에서 선택 가능하게 하고 운영에서는 활성화
- 입력 검증: Zod 또는 동급의 런타임 스키마
- 차트: SVG/Canvas 기반 차트 라이브러리 한 종류만 선택
- 지도: MapLibre GL JS
- 백그라운드 작업: 별도 TypeScript worker + PostgreSQL jobs 테이블
- 실시간 알림/채팅 MVP: PostgreSQL `LISTEN/NOTIFY` + SSE, 메시지 원본은 항상 테이블에서 재조회
- 테스트: 단위 테스트 러너 + Playwright E2E
- 패키지 관리자: pnpm
- 로컬 실행: Docker Compose로 web, worker, PostgreSQL/PostGIS

라이브러리 버전은 구현 시점의 호환 가능한 안정 버전을 고정하고 lockfile을 커밋한다.

### 8.2 저장소 구조

```text
app/
  (public)/
  (player)/
  admin/
  api/
src/
  auth/
  db/
    schema/
    migrations/
    queries/
  domain/
    campaign/
    economy/
    politics/
    research/
    events/
    diplomacy/
    map/
    chat/
  ai/
    providers/
    prompts/
    schemas/
    routing/
  jobs/
  ui/
worker/
  index.ts
  handlers/
tests/
  unit/
  integration/
  e2e/
scripts/
  seed.ts
  generate-map-grid.ts
docs/
  decisions/
```

도메인 계산을 React 컴포넌트나 Route Handler 안에 직접 쓰지 않는다. UI → 서버 명령 → 도메인 서비스 → 저장소 계층의 방향을 유지한다.

### 8.3 배포 단위

- `web`: UI, 인증, API, SSE
- `worker`: 턴 계산과 LLM 작업
- `postgres`: 상태, 공간 데이터, 작업 큐, 실시간 알림
- 선택 사항 `object-storage`: 사건 배경과 초상 이미지. MVP 초기에는 저장소의 정적 샘플 에셋 사용 가능

초기 운영은 단일 VPS 또는 장기 실행 Node 프로세스를 지원하는 환경을 우선한다. SSE와 worker를 사용하므로 함수 실행 시간이 짧은 순수 서버리스에 맞추기 위해 구조를 억지로 바꾸지 않는다.

---

## 9. 데이터 모델

모든 주요 테이블은 UUID 기본키, `created_at`, `updated_at`을 사용한다. 변경 이력이 중요한 스냅샷 테이블은 update 대신 insert를 기본으로 한다.

### 9.1 계정·캠페인

- `users`: 인증 사용자, 전역 역할, 상태
- `sessions`, `accounts`, `verifications`: 인증 라이브러리 테이블
- `campaigns`: 이름, 슬러그, 세계관, 활성 여부, 시간 설정, 규칙 버전, 지도 리비전
- `campaign_memberships`: 캠페인 역할과 상태
- `turns`: 순번, 게임 날짜 범위, 상태, 각 단계 완료 시각, 오류
- `country_applications`: 사용자 국가 배정 신청과 처리 상태

### 9.2 국가·배정

- `countries`: 캠페인, 국명, 코드, 색상, AI 여부, 설정 상태, 현재 공개 프로필 리비전
- `country_profile_revisions`: 국가상징, 역사, 지리, 단위 등 서술·정적 설정의 버전과 승인 상태
- `country_symbols`: 국기, 표어, 국가(國歌), 국목, 국화, 국조, 국수와 미디어 키
- `administrative_divisions`: 국가별 1~3단계 행정구역 트리, 명칭, 상위 구역
- `country_offices`: 국가원수, 총리/부통령, 의회 의장, 대법원장 등 직위와 재임 기간
- `country_units_and_codes`: 법정연호, 시간대, 도량형, ccTLD, 국가코드, 차량 통행 방향
- `country_assignments`: 국가, 사용자, 시작/종료 턴, 활성 여부
- 활성 국가-사용자 조합에는 유니크 제약을 둔다.
- `country_setup_submissions`: 사용자 빠른/고급 설정 초안, 검토 상태, 관리자 의견, 승인 리비전
- `country_flags`: 제한된 키-값 상태. 임의 대용량 JSON 저장소로 사용하지 않는다.

### 9.3 경제·정치

- `demographic_snapshots`: 총인구, 해외 자국인, 국내 외국인, 디아스포라, 출산율, 성장률, 기대수명, 평균연령, 파생 인구밀도
- `economic_snapshots`: 국가·턴별 GDP/GNI/GNP, 디플레이터, 국부, 외환, 화폐가치, 신용등급, 지니계수, CPI·토지가, 고용, 재정·채무와 계산 규칙 버전
- `economic_snapshot_inputs`: 각 계산 입력의 원본/추정/관리자 override 여부, 단위, 기준연도, 출처
- `economic_sectors`: 국가·턴·부문별 비중, 생산 지수, 생산성, 성장률
- `financial_institutions`: 주요 은행, 시스템 중요도, 건전성, 관련 산업
- `major_companies`: 주요 대기업, 산업, 규모 지수, 국가 소유 여부, 시스템 중요도
- `political_snapshots`: 국가·턴별 핵심 정치 지표와 당시 주요 직위
- `parties`: 국가별 정당 기본 정보
- `party_snapshots`: 정당·턴별 지지율, 의석, 조직력, 여야 상태
- `modifiers`: 대상, metric, 연산, 값, 시작/종료 턴, 출처
- `simulation_rules`: 버전별 계수와 허용 범위

### 9.4 연구·사건

- `tech_nodes`: 캠페인별 기술 정의
- `tech_prerequisites`: 선행 관계
- `country_research`: 국가별 진행 상태
- `research_allocations`: 턴별 포인트 배분
- `events`: 사건 본문, 이미지 키, 공개 범위, 상태, 출처
- `event_options`: 선택지와 도메인 효과
- `event_choices`: 국가/사용자 선택과 시각

### 9.5 연재·판정

- `submissions`: 사용자 제출 현재 버전, 상태, 분야
- `submission_versions`: 원문 버전 이력
- `judgment_runs`: 공급자 호출과 검증 기록
- `judgment_proposals`: 구조화된 제안과 관리자 수정본
- `effect_proposals`: 개별 변경 효과, 검증 상태, 승인 여부
- `applied_effects`: 실제 적용된 효과와 대상 스냅샷
- `review_comments`: 관리자 질문과 플레이어 답변

### 9.6 외교

- `country_relations`: 방향 또는 대칭 관계 수치와 태그
- `diplomatic_orientations`: 국가별 외교 성향, 공개 원칙, AI용 구조화 목표
- `international_organizations`: 국제기구 정의
- `organization_memberships`: 국가의 가입 국제기구, 가입/탈퇴 턴, 지위
- `diplomatic_proposals`: 발신/수신 국가, 유형, 상태, 유효기간, 공개 범위
- `diplomatic_messages`: 제안 협상 메시지
- `treaties`: 조약 상태, 당사국, 시작/종료 턴, 조건

### 9.7 지도

- `map_cells`: 불변 셀 ID, geometry, 육지/해양, 면적
- `map_ownership`: 캠페인, 지도 리비전, 셀, 국가
- `map_change_sets`: 기준 리비전, 새 리비전, 작업자, 사유
- `map_cell_changes`: 셀별 이전/새 소유국

데이터가 커지면 `map_ownership` 전체 복사 대신 현재 소유권 + 변경 로그 방식으로 최적화할 수 있다. MVP에서는 조회 성능과 복구 편의 중 구현이 단순한 방식을 선택하되 리비전 충돌 검사는 반드시 유지한다.

### 9.8 채팅·운영

- `chat_channels`: 캠페인 전체, 국가 내부, 공지
- `chat_channel_members`: 비공개 채널 멤버
- `chat_messages`: 원문, 답글 대상, 삭제 시각, 삭제자
- `moderation_actions`: 유형, 대상, 기간, 사유, 작업자
- `notifications`: 사용자 알림과 읽음 상태
- `jobs`: 유형, payload, 상태, 시도 수, 예약/잠금/완료 시각, idempotency key
- `audit_logs`: 작업자, 액션, 대상, 전/후 요약, IP/요청 ID

### 9.9 DB 필수 제약

- 비율과 지표의 `CHECK` 범위
- 관계도 `-100~100`
- 정치 지표 `0~100`
- 음수가 될 수 없는 금액·인구·연구 비용
- 캠페인 안에서 국가 코드와 기술 코드 유일
- 한 턴의 국가 스냅샷 유일
- 총면적보다 내수면적이 클 수 없고, 파생 인구밀도·1인당 소득·부채율은 원본값과 허용오차 안에서 일치
- 경제 금액에는 통화·배율·기준연도 누락 금지
- 승인된 국가 프로필 리비전은 국가별 한 시점에 하나만 현재 버전으로 지정
- 동일 idempotency key 유일
- 중요한 참조에는 삭제 제한. 운영 데이터는 기본적으로 soft delete

---

## 10. 서버 명령과 작업 큐

### 10.1 서버 명령 예시

- `applyForCountry`
- `assignCountry`
- `saveCountrySetupDraft`, `submitCountrySetup`, `approveCountrySetup`, `requestCountrySetupChanges`
- `createSubmission`, `submitSubmission`, `reviseSubmission`
- `lockTurn`, `startTurnCalculation`, `publishTurn`
- `approveJudgmentProposal`, `rejectJudgmentProposal`
- `allocateResearchPoints`
- `chooseEventOption`
- `sendDiplomaticProposal`, `respondToDiplomaticProposal`
- `saveMapChangeSet`
- `sendChatMessage`, `deleteChatMessage`, `timeoutUser`

각 명령은 인증 → 권한 → 입력 검증 → 현재 상태 검증 → 트랜잭션 → 감사 로그 → 알림 순서를 따른다.

### 10.2 작업 유형

- `CALCULATE_COUNTRY_ECONOMY`
- `CALCULATE_COUNTRY_RESEARCH`
- `JUDGE_SUBMISSION`
- `GENERATE_OPPOSITION_ACTION`
- `GENERATE_AI_DIPLOMACY_RESPONSE`
- `GENERATE_TURN_EVENT`
- `FINALIZE_TURN_REVIEW_DATA`

worker는 `FOR UPDATE SKIP LOCKED` 방식으로 작업을 가져오고, heartbeat와 잠금 만료를 사용해 중단된 작업을 복구한다. 작업 payload에는 대용량 국가 스냅샷을 복제하지 말고 확정된 ID를 저장한다.

---

## 11. UI·비주얼 지침

원하는 분위기는 “어두운 작전실 + 국가 보고서 + 비주얼 노벨 사건”이다.

- 배경: 차콜·짙은 청회색
- 표면: 금속 패널처럼 계층이 보이는 2~3단계 명도
- 강조색: 캠페인 주색과 각 국가색을 제한적으로 사용
- 성공/경고/위험 색은 색맹 사용자도 아이콘과 문구로 구분
- 제목은 개성 있는 디스플레이 서체, 본문·표는 가독성 높은 한글 UI 서체
- 장식용 CRT 노이즈·스캔라인은 기본 약하게, 설정에서 끌 수 있게 한다.
- 빠른 깜빡임, 과도한 글로우, 낮은 대비는 금지한다.
- 수치 테이블은 우측 정렬, 고정 소수 자릿수, 단위 표시를 일관되게 유지한다.
- 모든 로딩에는 스켈레톤 또는 진행 상태, 모든 빈 상태에는 다음 행동을 표시한다.
- 관리자 화면은 미려함보다 비교·필터·diff의 정보 밀도를 우선한다.

이벤트 화면은 독자 에셋을 사용한다. 다른 게임의 로고, 초상, 배경, 아이콘, 고유 문구, UI 프레임을 복사하지 않는다.

---

## 12. 보안·운영 요구사항

- 비밀번호는 인증 라이브러리의 안전한 해싱 구현을 사용한다.
- 세션 쿠키는 운영에서 `HttpOnly`, `Secure`, 적절한 `SameSite`를 사용한다.
- 모든 Server Action/Route Handler에서 인증과 캠페인 권한을 다시 확인한다.
- 입력은 서버에서 스키마 검증한다. 클라이언트 검증만 신뢰하지 않는다.
- 채팅과 연재 마크다운은 allowlist sanitizer를 거친다.
- 로그인, 채팅, 제출, LLM 재생성, 지도 저장에 속도 제한을 둔다.
- LLM API 키는 서버 전용 환경변수이며 브라우저 번들·로그·오류 응답에 노출하지 않는다.
- LLM 입력에서 불필요한 이메일, IP, 세션 정보 등 개인정보를 제외한다.
- 관리자 수동 변경, 판정 승인, 역할 변경, 제재, 지도 변경은 감사 로그 필수다.
- DB 백업과 복구 절차를 문서화한다. 최소 일 1회 백업과 공개 직전 스냅샷을 권장한다.
- 개발·테스트에서는 실제 LLM을 기본 호출하지 않고 `FakeAIProvider`를 사용한다.

---

## 13. 테스트 요구사항

### 13.1 단위 테스트

- 경제 공식의 정상·불황·고물가·음수 값 경계
- 국가 원장 경제 입력 각각의 영향 경로와 중복 효과 방지
- 인구밀도·1인당 GDP/GNI·정부지출 증가율·부채율 파생값 정합성
- GDP, 재정수지, 채무의 회계 일관성
- 정치 지표 clamp와 정당 지지율 정규화
- 기술 트리 순환 탐지와 선행 조건
- 효과 allowlist, 범위, 기간 검증
- LLM 출력 스키마와 공급자 fallback 라우팅
- 턴 단계와 idempotency
- 지도 리비전 충돌

### 13.2 통합 테스트

- 사용자 가입 → 국가 신청 → 관리자 배정
- 빠른 국가 설정 제출 → 관리자 승인 → 초기 인문·경제·정치 스냅샷 생성
- 제출 잠금 후 수정 차단
- worker 작업 획득·재시도·중복 방지
- 판정 승인 전 DB 미반영, 승인 후 1회 반영
- 턴 공개 시 스냅샷 생성
- 타임아웃 사용자 채팅 작성 차단
- 관리자 아닌 사용자의 관리자 API 접근 거절

### 13.3 Playwright E2E

최소 5개 핵심 시나리오를 유지한다.

1. 플레이어 로그인 후 국가 개요의 핵심 카드와 분야별 `세부 지표 보기` 확인
2. 연재 작성·제출·상태 확인
3. 지도에서 타국 선택 후 외교 제안 전송
4. 관리자가 AI 판정 diff를 수정·승인
5. 관리자가 턴을 공개한 뒤 플레이어에게 새 사건과 지표가 표시됨

E2E는 실제 사용자에게 보이는 역할·텍스트·상태를 검증하고 구현 내부 클래스명에 의존하지 않는다.

---

## 14. 시드 데이터

개발 환경에 다음을 제공한다.

- 캠페인 1개, 턴 3개(2개 공개, 1개 진행 중)
- 국가 4개: 인간 담당 2, AI 2
- 국가별 상징·역사·행정구역·지리·인문·단위·외교 원장 샘플
- 관리자, 운영자, 플레이어 2명, 미배정 사용자 1명
- 국가별 8턴 경제·정치 추세
- 빠른 국가 설정 초안 1개와 승인된 초기 설정 1개
- 정당 3~5개씩
- 분야별 기술 20개 이상과 유효한 선행 관계
- 진행 사건 3개, 과거 사건 5개
- 제출 예시: 성공, 부분 성공, 추가 정보 필요, 실패
- 인간 외교 제안과 AI 응답 예시
- 글로벌·국가 채팅 샘플
- 작은 테스트용 헥사곤 지도와 선택 가능한 전체 지도 생성 스크립트

시드용 비밀번호와 계정은 README에 개발 전용임을 명시하고 운영 빌드에서는 생성하지 않는다.

---

## 15. 단계별 구현 계획

각 단계는 독립적으로 실행 가능한 상태로 끝낸다.

### Phase 0 — 저장소와 실행 기반

- Next.js/TypeScript/pnpm 초기화
- 환경변수 검증, Docker Compose, PostgreSQL/PostGIS 연결
- Drizzle 스키마와 마이그레이션 기반
- 테스트·린트·타입검사·포맷 명령
- 기본 레이아웃과 디자인 토큰

완료 기준: 새 개발자가 문서대로 한 번에 로컬 실행하고 health check와 DB 마이그레이션을 통과한다.

### Phase 1 — 인증, 캠페인, 국가 배정

- 가입/로그인/로그아웃
- 역할·캠페인 멤버십·서버 권한 가드
- 국가/캠페인 시드
- 국가 신청, 관리자 배정
- 플레이어 `빠른 국가 설정` 10개 필드, 관리자 승인, 초기 프로필 리비전
- 플레이어 대시보드·국가 개요 뼈대와 관리자 홈

완료 기준: 배정되지 않은 사용자는 국가 페이지에 접근할 수 없고, 배정된 사용자의 빠른 설정은 관리자 승인 후에만 초기 국가 상태가 된다.

### Phase 2 — 경제·정치·연구 읽기/관리

- 전체 국가 원장, 인문·경제·정치 스냅샷, 정당·기술 데이터 모델
- 파생값·단위·기준연도 정합성 검증
- 플레이어 국가 개요/역사·지리/인문/경제/정치/연구 페이지
- 페이지별 핵심 카드 최대 10개와 `세부 지표 보기`/`계산 근거 보기`
- 관리자 경제/정치 일괄 편집과 diff
- 기술 트리와 연구 배분
- 전체 국가 원장 수치를 사용하는 결정론적 경제 계산기와 영향 경로 단위 테스트

완료 기준: 관리자가 값을 안전하게 수정하고, 모든 경제 입력이 계산 경로를 가지며, 플레이어는 핵심 정보만 먼저 보고 필요할 때 세부 추세와 계산 근거를 펼칠 수 있다.

### Phase 3 — 연재, 판정, 턴 처리

- 연재 작성·버전·제출·잠금
- jobs worker와 FakeAIProvider
- 실제 공급자 어댑터, 구조화 출력 검증, fallback
- 관리자 3열 판정 검토 화면
- 턴 상태 머신, 승인 효과 적용, 스냅샷 공개

완료 기준: 한 턴을 처음부터 공개까지 진행하고 재실행해도 효과가 중복되지 않는다.

### Phase 4 — 사건과 야당 행동

- 사건·선택지·효과 모델
- 비주얼 노벨형 사건 UI
- 사건 선택과 턴 반영
- 야당 행동 생성·검토·공개

완료 기준: 자동 생성된 정치 사건을 관리자가 검토하고 플레이어 선택이 다음 턴에 반영된다.

### Phase 5 — 지도와 외교

- MapLibre 읽기 지도와 국가 선택
- PostGIS 헥사곤 생성·벡터 타일
- 관리자 브러시 편집, 리비전, 충돌 처리
- 인간 외교 제안과 AI 국가 응답

완료 기준: 타국 클릭 → 제안 → AI/인간 답변이 작동하고, 지도 변경은 저장·감사·충돌 방지가 된다.

### Phase 6 — 채팅과 운영 강화

- 캠페인/국가/공지 채널
- SSE 실시간 알림과 메시지 페이지네이션
- 삭제, 타임아웃, 제재 화면
- AI 작업 모니터링, 속도 제한, 감사 로그 조회

완료 기준: 여러 브라우저에서 메시지가 갱신되고 타임아웃과 삭제가 서버에서 강제된다.

### Phase 7 — MVP 마감

- 핵심 Playwright 시나리오
- 접근성·키보드·대비 점검
- 성능 점검: 대형 테이블, 지도 타일, LLM 큐
- 백업/복구·배포·운영 문서
- 전체 시드 데모와 known limitations

완료 기준: 프로덕션과 유사한 환경에서 전체 게임 루프 데모를 실패 없이 1회 수행한다.

---

## 16. Codex 작업 방식

각 작업 세션에서 다음 형식을 따른다.

1. 현재 단계와 사용자 목표를 한 문장으로 재확인한다.
2. 관련 파일, 스키마, 테스트, 기존 변경을 검사한다.
3. 3~7개 이하의 짧은 구현 계획을 제시한다.
4. 가장 작은 수직 기능 단위로 코드와 테스트를 함께 작성한다.
5. DB 변경에는 마이그레이션과 시드 호환성을 포함한다.
6. UI 변경은 로딩·빈 상태·오류·권한 없음 상태를 함께 구현한다.
7. 완료 후 실행한 명령과 통과/실패 결과를 보고한다.
8. 남은 위험과 다음 한 단계를 짧게 제시한다.

### 금지 사항

- LLM 출력 문자열을 파싱해 임의 SQL 실행
- 관리자 UI에서만 권한을 막고 API는 열어두기
- 턴 스냅샷을 제자리 update로 덮어쓰기
- 금액에 JavaScript `number` 산술 사용
- 지도 전체 GeoJSON을 매 요청마다 내려보내기
- 모델 ID, API 키, 캠페인 ID, 사용자 ID 하드코딩
- 오류를 삼키고 성공 토스트 표시
- 테스트 통과를 위해 핵심 검증 비활성화
- 사용자 요구 없이 전쟁, 결제, 모바일 등 범위 확장

---

## 17. 환경변수 초안

```dotenv
DATABASE_URL=
BETTER_AUTH_SECRET=
APP_BASE_URL=

JUDGMENT_DEFAULT_PROVIDER=deepseek
JUDGMENT_DEFAULT_MODEL=
JUDGMENT_FALLBACK_PROVIDER=openai
JUDGMENT_FALLBACK_MODEL=gpt-5.6-luna
JUDGMENT_COMPLEX_PROVIDER=openai
JUDGMENT_COMPLEX_MODEL=gpt-5.6-terra
JUDGMENT_CHEAP_PROVIDER=
JUDGMENT_CHEAP_MODEL=

DEEPSEEK_API_KEY=
OPENAI_API_KEY=
AI_REQUEST_TIMEOUT_MS=60000
AI_MAX_RETRIES=2

MAP_HEX_EDGE_METERS=20000
APP_DEFAULT_LOCALE=ko
```

실제 `.env`는 커밋하지 않는다. `.env.example`에는 빈 값과 설명만 둔다.

---

## 18. MVP 완료 체크리스트

- [ ] 가입·로그인·로그아웃·세션 만료가 작동한다.
- [ ] 서버 권한 테스트가 USER/PLAYER/MODERATOR/ADMIN을 구분한다.
- [ ] 국가 신청과 관리자 배정이 작동한다.
- [ ] 사용자가 핵심 10개 필드로 국가를 빠르게 설정하고 관리자가 승인할 수 있다.
- [ ] 전체 국가 원장이 분야별로 저장되지만 플레이어 첫 화면에는 핵심 정보만 표시된다.
- [ ] 정치·경제·인문 페이지의 `세부 지표 보기`와 `계산 근거 보기`가 작동한다.
- [ ] 경제·정치·연구 데이터를 플레이어와 관리자가 각 권한에 맞게 본다.
- [ ] 제공된 인문·경제 수치가 다음 턴 계산에 실제로 사용되며 영향 경로 테스트가 있다.
- [ ] 경제 계산이 결정론적이고 설명 가능한 기여도를 남긴다.
- [ ] 1인당 지표·인구밀도·부채율·지출 증가율이 원본값에서 일관되게 파생된다.
- [ ] 기술 선행 조건과 연구 포인트가 검증된다.
- [ ] 연재 버전, 제출, 잠금, 판정, 승인, 공개가 연결된다.
- [ ] LLM 실패와 거절이 대체 공급자 또는 관리자 수동 처리로 전환된다.
- [ ] LLM이 DB를 직접 수정할 수 없다.
- [ ] 턴 재실행이 중복 효과를 만들지 않는다.
- [ ] 사건과 선택지가 비주얼 노벨 형태로 표시되고 결과가 반영된다.
- [ ] AI 국가 외교 답변과 야당 행동이 관리자 검토를 거친다.
- [ ] 타국 클릭과 외교 제안이 작동한다.
- [ ] 헥사곤 지도가 타일 단위로 로드되고 관리자 편집 이력이 남는다.
- [ ] 채팅, 삭제, 타임아웃, 속도 제한이 작동한다.
- [ ] 감사 로그가 핵심 관리자 작업을 추적한다.
- [ ] 5개 핵심 E2E가 통과한다.
- [ ] 백업·복구·배포·운영 문서가 있다.

---

## 19. 나중에 결정할 항목

이 항목은 MVP 구현을 시작하는 데 방해되지 않는다.

- 캠페인 공식 명칭과 세계관 연도
- 게임 시간 진행량의 실제 운영 주기
- 경제 계수의 밸런스
- 기술 분야와 노드 수
- 사용자 국가 생성 허용 방식
- AI의 저위험 자동 승인 범위
- 사건 이미지 제작·업로드 워크플로
- 향후 전쟁/군사 모델
- 모바일 UI 우선순위

---

## 20. 첫 Codex 세션에 붙여 넣을 실행 프롬프트

```text
저장소의 virtual-nation-mvp-codex-guide.md를 이 프로젝트의 제품·기술 기준으로 사용해라.

이번 작업은 Phase 0만 수행한다. 먼저 저장소와 기존 변경을 확인하고, Next.js + TypeScript + pnpm, PostgreSQL/PostGIS, Drizzle, 테스트 기반, Docker Compose, 환경변수 검증, 기본 레이아웃을 구성하라.

지침서 밖의 기능을 미리 구현하지 마라. 다만 다음 단계가 안전하게 확장될 수 있도록 폴더 경계와 DB 마이그레이션 구조는 마련하라. 기존 사용자 변경을 덮어쓰지 말고, 구현 전 짧은 계획을 제시하라. 구현 후 설치·마이그레이션·타입검사·린트·테스트·로컬 실행을 검증하고 결과와 남은 문제를 보고하라.
```

Phase 0 이후에는 마지막 문단의 `Phase 0`을 다음 단계로 바꾸고, 해당 단계의 완료 기준만 추가해 순차 진행한다.

---

## 21. 구현 참고 문서

- [Next.js App Router](https://nextjs.org/docs/app)
- [Next.js 데이터 변경 보안 지침](https://nextjs.org/docs/app/getting-started/mutating-data)
- [Better Auth 이메일·비밀번호 인증](https://better-auth.com/docs/authentication/email-password)
- [Drizzle PostgreSQL](https://orm.drizzle.team/docs/get-started-postgresql)
- [Drizzle PostgreSQL 확장/geometry](https://orm.drizzle.team/docs/extensions)
- [MapLibre GL JS](https://maplibre.org/maplibre-gl-js/docs/)
- [PostGIS 문서](https://postgis.net/docs/)
- [PostgreSQL LISTEN](https://www.postgresql.org/docs/current/sql-listen.html) / [NOTIFY](https://www.postgresql.org/docs/current/sql-notify.html)
- [OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs)
- [OpenAI Responses API 전환 안내](https://developers.openai.com/api/docs/guides/migrate-to-responses)
- [GPT-5.6 Luna 모델](https://developers.openai.com/api/docs/models/gpt-5.6-luna)
- [GPT-5.6 Terra 모델](https://developers.openai.com/api/docs/models/gpt-5.6-terra)
- [Playwright](https://playwright.dev/docs/intro)
